# Tesseract 日本語データの手動インストール手順

Tesseractのインストーラーに日本語データが含まれていない場合の対処方法です。

## ⚠️ 前提条件: Tesseractが認識されるか確認

まず、PowerShellでTesseractが認識されるか確認してください：

```powershell
tesseract --version
```

### エラーが出る場合: tesseractコマンドが認識されません

以下のエラーが出る場合は、環境変数PATHの設定が必要です：
```
tesseract: The term 'tesseract' is not recognized...
```

**解決方法: 環境変数に追加**

1. **Windowsキー**を押して「環境変数」と入力
2. 「システム環境変数の編集」をクリック
3. 「環境変数(N)...」ボタンをクリック
4. 「システム環境変数」の「Path」を選択して「編集(I)...」をクリック
5. 「新規(N)」をクリック
6. 以下を入力：
   ```
   C:\Program Files\Tesseract-OCR
   ```
7. 「OK」を3回クリックして閉じる
8. **PowerShellを再起動**（重要！）
9. 再度確認：
   ```powershell
   tesseract --version
   ```

バージョン情報が表示されればOKです。次のステップに進んでください。

---

## 📥 日本語データファイルのダウンロード

### 方法1: GitHubから直接ダウンロード（推奨）

1. **ブラウザで以下のURLにアクセス**:
   ```
   https://github.com/tesseract-ocr/tessdata/raw/main/jpn.traineddata
   ```

2. **ファイルが自動的にダウンロードされます**
   - ファイル名: `jpn.traineddata`
   - サイズ: 約15MB

3. **ダウンロードフォルダから移動**:
   - ダウンロードした `jpn.traineddata` ファイルを以下のフォルダにコピー:
   ```
   C:\Program Files\Tesseract-OCR\tessdata\
   ```

### 方法2: PowerShellでダウンロード（コマンドライン）

1. **PowerShellを管理者権限で開く**:
   - スタートメニューで「PowerShell」を検索
   - 右クリック → 「管理者として実行」

2. **以下のコマンドを実行**:
   ```powershell
   # tessdataフォルダに移動
   cd "C:\Program Files\Tesseract-OCR\tessdata"

   # 日本語データをダウンロード
   Invoke-WebRequest -Uri "https://github.com/tesseract-ocr/tessdata/raw/main/jpn.traineddata" -OutFile "jpn.traineddata"
   ```

3. **ダウンロード完了を確認**:
   ```powershell
   dir jpn.traineddata
   ```

### 方法3: コマンドプロンプトでダウンロード

1. **コマンドプロンプトを管理者権限で開く**:
   - スタートメニューで「cmd」を検索
   - 右クリック → 「管理者として実行」

2. **以下のコマンドを実行**:
   ```cmd
   cd "C:\Program Files\Tesseract-OCR\tessdata"
   curl -L -o jpn.traineddata https://github.com/tesseract-ocr/tessdata/raw/main/jpn.traineddata
   ```

## ✅ インストール確認

### 1. ファイルの存在確認

コマンドプロンプトまたはPowerShellで以下を実行:

```cmd
dir "C:\Program Files\Tesseract-OCR\tessdata\jpn.traineddata"
```

以下のような出力が表示されればOK:
```
2024/XX/XX  XX:XX        15,111,979 jpn.traineddata
```

### 2. Tesseractで日本語が使用可能か確認

```cmd
tesseract --list-langs
```

出力に `jpn` が含まれていればインストール成功:
```
List of available languages (2):
eng
jpn
```

## 🔧 トラブルシューティング

### エラー: アクセスが拒否されました

**原因**: tessdataフォルダへの書き込み権限がない

**解決方法**:
1. PowerShellまたはコマンドプロンプトを**管理者権限**で起動
2. または、デスクトップにダウンロードしてから手動でコピー:
   ```cmd
   # デスクトップにダウンロード
   cd %USERPROFILE%\Desktop
   curl -L -o jpn.traineddata https://github.com/tesseract-ocr/tessdata/raw/main/jpn.traineddata

   # 管理者権限でコピー（別のコマンドプロンプトを管理者として開く）
   copy "%USERPROFILE%\Desktop\jpn.traineddata" "C:\Program Files\Tesseract-OCR\tessdata\"
   ```

### エラー: curl/Invoke-WebRequest が見つかりません

**Windows 10以前の場合**:
- 方法1（GitHubから直接ダウンロード）を使用してください
- ブラウザでダウンロードしてからコピーする方が簡単です

### ファイルをコピーしても jpn が表示されない

**確認ポイント**:
1. ファイル名が正確に `jpn.traineddata` になっているか
   - ダウンロード時に `.txt` などの拡張子が追加されていないか確認
   - 正しいファイル名: `jpn.traineddata`
   - 間違い: `jpn.traineddata.txt`

2. ファイルが正しいフォルダに配置されているか:
   ```
   C:\Program Files\Tesseract-OCR\tessdata\jpn.traineddata
   ```

3. ファイルサイズが正しいか:
   - 約15MB（15,111,979 bytes）
   - サイズが極端に小さい場合は再ダウンロード

## 📋 その他の言語データ

英語以外の言語が必要な場合も同じ手順で追加できます:

### よく使われる言語

- **英語** (デフォルトで含まれる): `eng.traineddata`
- **中国語簡体字**: `chi_sim.traineddata`
- **中国語繁体字**: `chi_tra.traineddata`
- **韓国語**: `kor.traineddata`

### ダウンロードURL

```
https://github.com/tesseract-ocr/tessdata/raw/main/{言語コード}.traineddata
```

例:
- 中国語簡体字: `https://github.com/tesseract-ocr/tessdata/raw/main/chi_sim.traineddata`
- 韓国語: `https://github.com/tesseract-ocr/tessdata/raw/main/kor.traineddata`

## 🎯 次のステップ

日本語データのインストールが完了したら:

1. **アプリをビルド**:
   ```cmd
   flet build windows
   copy_tesseract_windows.bat
   ```

2. **動作確認**:
   ```cmd
   cd build\windows
   SummaryForDoc.exe
   ```

3. 画像ファイル（.jpg, .png）を読み込んでOCRが正常に動作することを確認

## 📖 関連ドキュメント

- [WINDOWS_QUICKSTART.md](WINDOWS_QUICKSTART.md) - クイックスタートガイド
- [WINDOWS_BUILD.md](WINDOWS_BUILD.md) - 詳細なビルド手順
- [Tesseract公式リポジトリ](https://github.com/tesseract-ocr/tessdata) - 他の言語データ
